package nam.model.connectionPool;

import java.io.Serializable;
import java.util.Collection;

import javax.enterprise.context.ApplicationScoped;
import javax.faces.component.UIComponent;
import javax.faces.context.FacesContext;
import javax.faces.validator.Validator;
import javax.faces.validator.ValidatorException;
import javax.inject.Named;

import org.aries.ui.validate.AbstractValidator;

import nam.model.ConnectionPool;
import nam.model.util.ConnectionPoolUtil;


@ApplicationScoped
@Named("connectionPoolValidator")
public class ConnectionPoolValidator extends AbstractValidator implements Validator, Serializable {
	
	@Override
	public void validate(FacesContext facesContext, UIComponent uiComponent, Object value) throws ValidatorException {
		resetState();
		if (value != null) {
			if (value instanceof ConnectionPool == false)
				throwError("Unexpected value: "+value);
			ConnectionPool connectionPool = (ConnectionPool) value;
			if (ConnectionPoolUtil.validate(connectionPool) == false) {
				Collection<String> messages = getValidator().getMessages();
				getDisplay().addErrors(messages);
				throwErrors(messages);
			}
		}
	}
	
}
